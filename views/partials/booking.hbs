<div class="fecha">{{fecha}}</div>
<div class="user-name">{{userFound.username}}, Your events:</div>
<ul class="event-list-bookings">
  {{#each events}}
  <li>{{this.name}}</li>
  {{/each}}
</ul>
<p>booking partial</p>